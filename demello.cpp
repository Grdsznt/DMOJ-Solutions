#include <bits/stdc++.h>

using namespace std;

string input = "";

int main() {
    cin >> input;


    if (input.find("demello") != -1) {
        cout << "liar" << endl;
    } else {
        cout << "what are we going to do?" << endl;
    }
}
